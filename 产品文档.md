# 字己 · 产品功能使用手册

> **字己** 是一款坚持本地优先的个人文字档案应用。用文字记录自己，在回看中认识自己和理解自己，发现生命的留痕。

---

## 目录

1. [快速上手：三步开启文字归档](#1-快速上手三步开启文字归档)
2. [写作与编辑](#2-写作与编辑)
3. [浏览、检索与筛选](#3-浏览检索与筛选)
4. [文章阅读与回看](#4-文章阅读与回看)
5. [侧边栏与个性化空间](#5-侧边栏与个性化空间)
6. [数据存储与安全机制](#6-数据存储与安全机制)
7. [常见问题（FAQ）](#7-常见问题faq)

---

## 1. 快速上手：三步开启文字归档

字己的设计原则是**极简、轻量、无需网络**——无需注册账号，无需登录，数据完全存在本地。

### 步骤 1：指定本地文字存档目录
首次打开字己，点击左侧栏的 **「选择文件夹」**，在电脑中选择一个你希望存放文章的本地文件夹（例如 `D:\我的文字档案`，或放在 OneDrive/iCloud 等本地同步文件夹中）。

### 步骤 2：写下第一篇文字
点击左侧栏的 **「写文章」**（或按快捷键 `Ctrl + N`），即可进入专注写作界面。

### 步骤 3：保存与自动归档
输入文章标题、选择分类或添加标签，完成正文后点击右下角的 **「保存」**（或按快捷键 `Ctrl + S`）。文字会立即以通用的 `.md` 文件写入你指定的文件夹。

---

## 2. 写作与编辑

字己提供了一个安静、无干扰的 Markdown 纯文字书写环境。

### 2.1 专注写作与全屏模式
- **自动收起侧边栏**：进入编辑页面时，左侧栏会自动收起，让你将所有注意力集中在眼前的文字上；
- **全屏写作模式**：点击右下角工具栏的 **「全屏」** 按钮，可一键切换到沉浸式全屏视图；
- **实时字数统计**：工具栏右侧实时显示当前正文字数。

### 2.2 Markdown 快捷排版与键盘增强
支持完整的 Markdown 标记语法，并内置快捷键与智能续行：

| 快捷键 | 功能说明 | 快捷键 | 功能说明 |
|--------|----------|--------|----------|
| `Ctrl + S` | 保存文章到本地文件 | `Ctrl + N` | 新建空白文章 |
| `Ctrl + B` | **粗体** (`**文本**`) | `Ctrl + I` | *斜体* (`*文本*`) |
| `Ctrl + K` | 插入超链接 (`[链接](url)`) | `Ctrl + Shift + X` | ~~删除线~~ (`~~文本~~`) |
| `Ctrl + Shift + C` | 行内代码 / 多行代码块 | `Ctrl + Shift + B` | 引用区块 (`> 引用`) |
| `Ctrl + Shift + L` | 插入无序列表 (`- 项`) | `Ctrl + Shift + O` | 插入有序列表 (`1. 项`) |
| `Ctrl + Shift + H` | 插入标题 (`# 标题`) | `Ctrl + Shift + I` | 插入图片语法 |
| `Esc` | 退出编辑，返回列表 | `Ctrl + F` | 聚焦到顶部搜索框 |

- **智能列表续行**：在写列表时按 `Enter` 键会自动生成下一行列表标记，数字序号自动递增；空行回车自动取消列表；
- **Tab 智能缩进**：按 `Tab` 键缩进 2 个空格，按 `Shift + Tab` 减少缩进。

### 2.3 分类与多标签组织
- **添加分类**：点击底部工具栏「添加分类」，可从已有分类列表中快速选择，或点击「+ 添加新分类」创建；
- **标签管理**：点击底部工具栏「标签」，可勾选已有标签，或输入新标签按回车直接添加。

### 2.4 草稿实时防丢
打字时系统会实时暂存未保存的内容。如果发生误关窗口或异常退出，重新打开编辑页时会弹出 **「检测到未保存的草稿」** 提示，点击 **「恢复草稿」** 即可一键还原。点击保存或取消后，临时缓存会自动清除。

---

## 3. 浏览、检索与筛选

当记录逐渐累积，字己提供了直观、多角度的定位与回顾方式：

### 3.1 毫秒级全文检索
- 在顶部搜索框输入文字，系统会在 **300ms** 内实时模糊匹配所有文章的**标题、正文和标签**；
- 不区分英文字母大小写。

### 3.2 真实排序方式
文章列表支持以下 3 种排序模式，点击右上角下拉框即可切换：
1. **最新发布**（按文章创建日期从新到旧，默认）
2. **最早发布**（按文章创建日期从旧到新）
3. **标题排序**（按文章标题 A-Z 首字母排序）

### 3.3 分类、标签与年份多重筛选
- **分类筛选**：列表顶部支持在「全部」、「未分类」及自定义分类之间一键切换；
- **标签筛选**：点击标签云中的标签，可直接聚焦过滤出所有包含该标签的文章；
- **年份筛选**：通过年份下拉框，按年份（如 2026、2025 年）快速回溯历史文字。

---

## 4. 文章阅读与回看

点击列表中的文章卡片即可进入详情阅读页：

- **排版渲染**：正文使用 Markdown 引擎渲染，标题、引用、代码、加粗等样式清晰舒适；
- **文章元信息**：顶部完整展示所属分类、标签、创作日期与字数；
- **快速操作**：顶部提供「编辑」与「删除」按钮；
- **连续阅读**：底部提供 **「上一篇」与「下一篇」** 导航，方便按时间连续翻阅历史记录。

---

## 5. 侧边栏与个性化空间

### 5.1 8 套内置精美主题
点击左侧栏底部的配色色块，可一键切换 8 种主题风格：
- **绿野**（默认清新深绿）
- **嫩绿**
- **天蓝**
- **柔紫**
- **粉红**
- **暖黄**
- **暖纸**（复古纸质感）
- **墨黑**（沉浸深色模式）

### 5.2 写作活跃度日历
- 侧边栏内置类似 GitHub 风格的年度活跃度热力图，记录每日写作字数与活跃度；
- 直观统计：**本月字数**、**本年字数**、**连续写作天数**；
- 支持切换不同年份查看历史活跃轨迹。

### 5.3 树形头像与每日问候语
- **头像相册**：支持点击左上角头像更换个人自定义图片；
- **每日问候语**：顶部云朵气泡内置 20+ 条治愈系问候语，点击气泡即可随机刷新，启发今日书写灵感。

### 5.4 侧边栏折叠
点击侧边栏收起按钮，可随时隐藏左侧栏，让主内容区视野更开阔。

---

## 6. 数据存储与安全机制

**“数据完全归你所有，应用不截留任何数据”**。

### 6.1 开放的标准 Markdown 文件
每篇文章均为标准的 `.md` 文件，保存在你指定的本地目录中：

```markdown
---
title: "今天的记录"
category: "日记"
tags: ["情绪", "自我观察"]
date: "2026-06-11"
word_count: 300
---

正文内容...
```

- **免导出，随时可用**：脱离字己，你也可以随时用系统记事本、VS Code、Typora 或 Obsidian 直接打开阅读；
- **换机无门槛**：换电脑时直接把该文件夹拷走即可。

### 6.2 编辑自动历史备份
每次保存已有文章时，系统会在文章文件夹下的 `.selfrblog-backups/` 目录中自动生成一份时间戳备份快照（例如 `20260611-102030-文章名.md`），误改时可随时找回。

### 6.3 本地安全回收站
在字己中点击删除文章时，文件不会被直接粉碎，而是移入文章文件夹下的 `.selfrblog-trash/` 目录，防止误删。

---

## 7. 常见问题（FAQ）

### Q1：字己需要联网吗？会上传我的文字吗？
**不需要联网，绝不上传。** 字己是纯本地桌面应用，无云端服务器，没有任何数据采集。

### Q2：如何在多台电脑间同步？
只需将文章存放文件夹选在网盘的本地同步目录（如 OneDrive、iCloud、坚果云、Dropbox 或 NAS 同步盘），即可实现多设备自动同步。

### Q3：如何将其他地方的 Markdown 笔记导入字己？
直接将 `.md` 文件复制到你选择的文章文件夹中，在字己左侧栏点击 **「读取文章」** 即可。

### Q4：如何配合 AI 工具回看和理解自己？
由于所有文章均为本地纯文本，你可以自由将文章提供给你信任的个人 AI 工具（如 Claude、ChatGPT 等），让 AI 协助梳理过去一年的思维脉络或自我课题，全程完全由你自主掌控。
