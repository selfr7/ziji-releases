# 字己 产品文档 v2.0

## 目录

1. 产品说明
2. 产品定位
3. 目标用户
4. 核心竞争力
5. 产品形态与使用场景
6. 技术结构
7. 数据结构
8. 功能设计说明
9. 异常处理与安全策略
10. 版本功能清单
11. 后续规划建议

## 1. 产品说明

**字己**是一款本地优先的个人文字档案管理客户端。

它不是以公开发布、流量传播或社交互动为目标的内容平台，而是一个面向个人的长期写作空间。用户可以在自己的电脑里创建、保存、整理、检索和回看文字，通过持续记录形成一座私人的文字档案馆。

字己的核心价值是：**让用户用文字记录自己，并在回看文字的过程中重新理解自己。**

产品当前以 Markdown 文章为主要内容载体。每篇文章以 `.md` 文件保存在用户选择的本地文件夹中，文章标题、分类、标签、日期和字数等信息通过 Front Matter 写入文件。用户无需账号、无需联网、无需把数据上传到云端，即可完成个人文字资料的长期沉淀。

## 2. 产品定位

### 2.1 一句话定位

**字己，私人的文字档案馆。**

### 2.2 完整定位

字己是一款本地优先的个人文字档案管理客户端，面向需要长期记录、整理和回看自我文字的用户，提供 Markdown 写作、文章归档、分类标签、全文检索、写作统计和本地文件管理能力。

### 2.3 产品不是什么

字己不是传统意义上的博客系统，因为当前版本没有公开发布、分享链接、评论互动、订阅传播等功能。

字己也不是通用笔记软件。它不追求复杂知识库、双链、块编辑、团队协作或多端实时同步，而是聚焦于“个人文章式文字”的保存、整理和回看。

### 2.4 产品关键词

- 私人
- 本地
- 文字档案
- 自我记录
- 自我回看
- Markdown
- 长期写作
- 低干扰管理

## 3. 目标用户

### 3.1 核心用户

1. **个人写作者**

   需要一个安静、稳定、长期可用的地方写文章、随笔、札记或日记。

2. **自我觉察型用户**

   希望通过记录情绪、想法、经历和反思，持续观察自己的变化。

3. **Markdown 用户**

   习惯使用 Markdown 写作，希望文章保持开放文件格式，便于迁移、同步和备份。

4. **本地数据偏好者**

   不希望私人文字上传到平台或云端，更信任自己的本地文件夹。

5. **轻量归档用户**

   不需要复杂知识库，但需要对长期文字进行分类、打标签、搜索和回顾。

### 3.2 非核心用户

- 希望一键发布公开网站的用户
- 需要团队协作写作的用户
- 需要多端实时同步的用户
- 需要复杂数据库、任务管理或知识图谱的用户
- 主要写碎片便签而不是文章式内容的用户

## 4. 核心竞争力

### 4.1 本地优先，数据归用户所有

文章直接保存在用户选择的本地文件夹中。用户可以随时复制、备份、同步或用其他编辑器打开这些 Markdown 文件。产品不绑定账号，也不锁定数据格式。

### 4.2 私密写作，不以发布为默认目的

多数写作产品会暗示“写完要发布”。字己的默认场景是“写给自己、保存自己、回看自己”。这让产品气质更接近私人档案馆，而不是内容平台。

### 4.3 文章式档案管理

字己围绕文章而不是零散块内容设计。每篇内容都有标题、正文、日期、分类、标签和字数，适合沉淀阶段性想法、长期日记、主题随笔和个人复盘。

### 4.4 Markdown 开放格式

Markdown 文件可读、可迁移、可版本管理。即使脱离客户端，用户也能继续访问自己的文字。

### 4.5 写作记录可视化

写作日历把每日字数和写作活跃度可视化，让用户看到自己长期记录的轨迹。这种反馈服务于自我观察，而不是外部排名。

### 4.6 低干扰的整理能力

产品只提供必要的分类、标签、搜索和排序，不把用户推入复杂系统维护。它更适合长期使用，而不是短期尝鲜。

## 5. 产品形态与使用场景

### 5.1 产品形态

当前产品为 Windows 桌面客户端。应用启动后打开一个本地桌面窗口，前端页面通过本地服务访问用户选择的文章文件夹。

### 5.2 典型使用场景

1. **每日记录**

   用户每天打开字己，写下当天的经历、情绪或想法。

2. **阶段性复盘**

   用户通过搜索、分类、标签和日期回看某段时期的文字。

3. **主题文章沉淀**

   用户围绕某个主题持续写文章，并用分类和标签组织。

4. **私人资料归档**

   用户把过去写过的 Markdown 文件放入同一文件夹，通过字己统一浏览和管理。

5. **长期自我观察**

   用户通过写作日历看到自己何时写得多、何时中断、哪些时期更活跃。

## 6. 技术结构

## 6.1 技术栈

- 桌面壳：Electrobun
- 本地运行时：Bun
- 主进程语言：TypeScript
- 前端页面：HTML、CSS、JavaScript
- 文件格式：Markdown
- 元数据格式：YAML Front Matter
- Windows 渲染环境：WebView2
- 安装包：Inno Setup

## 6.2 应用结构

当前项目核心目录如下：

```text
selfrblog-desktop/
  public/
    index.html              # 前端页面、样式和交互逻辑
    images/                 # 前端静态图片
  src/
    main.ts                 # 桌面主进程、本地服务和 API
    storage.ts              # 本地文章文件读写、备份和删除
    storage.test.ts         # 存储逻辑测试
    bun/index.ts            # Electrobun 启动入口
  assets/                   # 图标、安装脚本模板等资源
  build/                    # 构建产物
  artifacts/                # 安装包和压缩包产物
```

## 6.3 运行机制

1. 应用启动。
2. 主进程设置 Windows DPI 感知。
3. 主进程查找 `public/index.html`。
4. Bun 启动本地 HTTP 服务，监听 `127.0.0.1` 的随机端口。
5. Electrobun 创建桌面窗口。
6. 桌面窗口加载本地服务地址。
7. 前端页面通过 `/api/*` 请求调用桌面端能力。
8. 桌面端负责文件夹选择、文章读取、文章保存、文章删除和静态资源响应。

## 6.4 前后端职责划分

### 前端职责

- 页面展示
- 文章列表渲染
- 文章详情渲染
- 编辑器交互
- 搜索、筛选、排序
- 分类和标签弹窗
- 草稿保存与恢复
- 写作日历展示
- 调用桌面端 API

### 桌面端职责

- 启动本地 HTTP 服务
- 打开系统文件夹选择窗口
- 保存当前文章文件夹配置
- 读取 Markdown 文件
- 写入 Markdown 文件
- 保存前备份旧文件
- 删除时移动到回收目录
- 限制文件访问路径，防止路径逃逸

## 6.5 桌面端 API

### `GET /api/health`

用于检测桌面端 API 是否可用。

响应示例：

```json
{
  "ok": true,
  "mode": "desktop"
}
```

### `GET /api/folder`

读取当前保存的文章文件夹。

响应示例：

```json
{
  "ok": true,
  "folder": {
    "path": "D:\\文字档案",
    "name": "文字档案"
  }
}
```

如果未选择文件夹：

```json
{
  "ok": true,
  "folder": null
}
```

### `POST /api/select-folder`

打开本机文件夹选择窗口，并保存用户选择的目录。

成功响应：

```json
{
  "ok": true,
  "folder": {
    "path": "D:\\文字档案",
    "name": "文字档案"
  }
}
```

取消响应：

```json
{
  "ok": false,
  "cancelled": true
}
```

### `GET /api/articles`

读取当前文章文件夹中的 Markdown 文件。

响应示例：

```json
{
  "ok": true,
  "articles": [
    {
      "fileName": "今天的记录.md",
      "content": "---\ntitle: \"今天的记录\"\n---\n\n正文"
    }
  ]
}
```

### `POST /api/articles`

保存文章。

请求示例：

```json
{
  "fileName": "今天的记录.md",
  "preferredName": "今天的记录.md",
  "content": "---\ntitle: \"今天的记录\"\ncategory: \"日记\"\ntags: [\"情绪\"]\ndate: \"2026-06-11\"\nword_count: 300\n---\n\n正文"
}
```

响应示例：

```json
{
  "ok": true,
  "fileName": "今天的记录.md"
}
```

### `DELETE /api/articles/:fileName`

删除指定文章。桌面端当前实现为移动到 `.selfrblog-trash` 目录。

响应示例：

```json
{
  "ok": true,
  "fileName": "今天的记录.md"
}
```

## 6.6 构建与安装包

### 开发运行

```bash
bun run dev
```

### Windows 构建

```bash
bun run build:win
```

### Windows 安装包

```bash
bun run package:win
```

主要产物：

- `artifacts/selfrblog-desktop-win-x64.zip`
- `artifacts/SelfrBlog-Setup-1.0.0.exe`

备注：当前构建产物名称仍沿用旧项目名，后续正式更名时应同步调整包名、应用名、安装目录和配置目录。

## 7. 数据结构

## 7.1 文章文件结构

字己的核心数据是 Markdown 文件。每篇文章由 Front Matter 和正文组成。

```markdown
---
title: "文章标题"
category: "分类"
tags: ["标签1", "标签2"]
date: "2026-06-11"
word_count: 1234
---

这里是正文内容。
```

## 7.2 Front Matter 字段说明

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `title` | string | 是 | 文章标题 |
| `category` | string | 否 | 文章分类；为空时可视为未分类 |
| `tags` | string[] | 否 | 文章标签数组 |
| `date` | string | 是 | 文章日期，格式为 `YYYY-MM-DD` |
| `word_count` | number | 是 | 正文字数 |

## 7.3 前端文章对象

前端读取文章后，会解析为文章对象。

```json
{
  "title": "文章标题",
  "category": "日记",
  "tags": ["情绪", "自我观察"],
  "date": "2026-06-11",
  "body": "正文内容",
  "wordCount": 1234,
  "fileName": "文章标题.md"
}
```

浏览器兼容模式下还可能包含：

```json
{
  "fileHandle": "FileSystemFileHandle"
}
```

## 7.4 草稿数据结构

草稿保存在浏览器本地存储 `localStorage` 中，键名为 `blog_draft`。

```json
{
  "title": "未保存的标题",
  "content": "未保存的正文",
  "category": "分类",
  "tags": "标签1，标签2",
  "editingFileName": "原文件名.md",
  "timestamp": 1781111111111
}
```

说明：

- `editingFileName` 存在时，表示草稿来自编辑已有文章。
- `editingFileName` 为空时，表示草稿来自新建文章。
- 保存文章成功后应清除草稿。

## 7.5 应用配置结构

桌面端配置文件保存当前文章文件夹。

```json
{
  "articleFolder": "D:\\文字档案"
}
```

当前配置目录仍为：

```text
selfrblog-desktop/config.json
```

后续产品正式更名时，应迁移为与“字己”一致的配置目录。

## 7.6 备份数据结构

保存已有文章时，桌面端会先备份旧文件。

备份目录：

```text
.selfrblog-backups/
```

备份文件命名：

```text
YYYYMMDD-HHMMSS-原文件名.md
```

示例：

```text
.selfrblog-backups/20260611-102030-今天的记录.md
```

## 7.7 回收站数据结构

删除文章时，桌面端会将文章移动到回收目录。

回收目录：

```text
.selfrblog-trash/
```

回收文件命名：

```text
YYYYMMDD-HHMMSS-原文件名.md
```

示例：

```text
.selfrblog-trash/20260611-103000-今天的记录.md
```

## 7.8 文件名规则

新建文章时，系统根据标题生成 Markdown 文件名。

规则：

- 移除 Windows 文件名非法字符：`\ / : * ? " < > |`
- 合并多余空格
- 去除首尾空格
- 自动添加 `.md`
- 标题为空时使用 `untitled.md`
- 同名文件存在时，桌面端自动追加序号，例如 `标题-2.md`

## 8. 功能设计说明

## 8.1 全局布局

### 功能目标

为用户提供稳定的三栏式信息层级：左侧为个人空间和统计，右侧为主要内容工作区。

### 页面结构

应用由两大区域组成：

- 侧边栏
- 主内容区

侧边栏包含：

- 树形头像
- 问候语
- 写文章入口
- 选择文件夹入口
- 读取文章入口
- 写作日历
- 写作统计

主内容区包含：

- 文章列表页
- 文章详情页
- 文章编辑页

### 交互规则

- 默认进入文章列表页。
- 进入编辑页时隐藏侧边栏，使用户专注写作。
- 返回列表或详情页时恢复常规布局。
- 用户可手动收起或展开侧边栏。

## 8.2 首次启动与文件夹选择

### 功能目标

让用户把本地某个文件夹指定为字己的文字档案库。

### 用户入口

- 侧边栏“选择文件夹”按钮

### 正常流程

1. 用户点击“选择文件夹”。
2. 系统打开系统文件夹选择窗口。
3. 用户选择一个文件夹。
4. 系统保存文件夹路径。
5. 系统将当前文件夹作为文章库。
6. 系统自动读取文件夹下的 Markdown 文件。
7. 系统更新文章列表、分类筛选和写作日历。

### 取消流程

1. 用户打开选择窗口。
2. 用户取消选择。
3. 系统不修改当前文件夹。
4. 系统提示或保持原状态。

### 状态规则

- 未选择文件夹时，写文章仍可进入编辑器，但保存时会走下载兜底或提示相关能力。
- 已选择文件夹时，按钮文案显示当前文件夹名称。
- 应用重启后尝试恢复上次选择的文件夹。

## 8.3 读取文章

### 功能目标

从本地文章库中读取 Markdown 文件，形成可浏览、可搜索、可统计的文章集合。

### 用户入口

- 选择文件夹后自动读取
- 侧边栏“读取文章”按钮
- 保存、删除文章后自动重新读取

### 读取范围

- 当前仅读取文章文件夹根目录下的 `.md` 文件。
- 当前不递归读取子目录。

### 解析规则

系统读取文件内容后，尝试解析 Front Matter。

只有包含有效 Front Matter 的文件会进入文章列表。至少需要识别到以下字段之一：

- `title`
- `category`
- `tags`
- `date`
- `word_count`

### 读取成功后更新

- 更新文章数组
- 按当前排序规则排序
- 渲染文章列表
- 渲染分类筛选标签
- 渲染写作日历
- 显示已加载文章数量

### 异常处理

- 单个文件读取失败时跳过该文件。
- 文件夹读取失败时提示用户。
- 无权限时提示重新选择文件夹。

## 8.4 文章列表

### 功能目标

提供文字档案库的浏览入口，帮助用户快速找到和打开文章。

### 展示字段

每篇文章卡片展示：

- 标题
- 标签，最多显示前 2 个
- 超出标签数量
- 日期
- 删除按钮

### 点击行为

- 点击文章卡片：进入文章详情页。
- 点击标签：按该标签筛选文章列表。
- 点击删除按钮：触发删除确认。

### 空状态

无文章时显示：

```text
还没有文章呢，请先选择文件夹或开始写作~
```

搜索或筛选无结果时显示：

```text
没有找到匹配的文章
```

## 8.5 搜索

### 功能目标

帮助用户通过关键词快速定位历史文字。

### 用户入口

- 文章列表顶部搜索框
- 快捷键 `Ctrl/Cmd + F`

### 搜索范围

- 文章标题
- 文章正文
- 文章标签

### 搜索规则

- 输入后 300ms 防抖执行搜索。
- 英文搜索不区分大小写。
- 搜索与当前分类或标签筛选条件叠加。
- 搜索为空时恢复当前筛选条件下的文章列表。

## 8.6 分类筛选

### 功能目标

通过文章分类帮助用户按主题或用途浏览文字。

### 分类来源

系统从已加载文章的 `category` 字段提取分类。

### 展示位置

文章列表标题旁的筛选标签区域。

### 交互规则

- 点击分类筛选标签后，列表只显示该分类文章。
- 再次点击同一分类可取消筛选。
- 点击“清除筛选”可恢复全部文章。
- 当前筛选状态会改变列表标题，例如 `分类：日记`。

## 8.7 标签筛选

### 功能目标

通过标签帮助用户建立跨分类的主题索引。

### 标签来源

系统从已加载文章的 `tags` 字段提取标签。

### 交互规则

- 点击文章卡片中的标签后，列表只显示包含该标签的文章。
- 当前筛选状态会改变列表标题，例如 `标签：情绪`。
- 标签筛选可通过“清除筛选”取消。

## 8.8 排序

### 功能目标

让用户按不同维度浏览文字档案。

### 排序选项

- 日期从新到旧
- 日期从旧到新
- 标题 A-Z
- 标题 Z-A

### 交互规则

- 用户切换排序选项后，当前列表立即重新排序。
- 排序结果受搜索和筛选条件影响。

## 8.9 文章详情页

### 功能目标

提供单篇文章的阅读、回看、编辑和删除入口。

### 展示字段

- 文章标题
- 文章日期
- 文章分类
- 文章标签
- 文章正文

### 操作入口

- 返回列表
- 编辑文章
- 删除文章

### 正文展示规则

- 当前版本以纯文本方式展示正文。
- 保留换行和空白。
- 当前版本不渲染 Markdown 为富文本 HTML。
- 正文字体与编辑器正文保持一致。

### 返回规则

- 点击返回按钮或按 `Esc` 返回文章列表。
- 返回后保留当前搜索或筛选上下文。

## 8.10 新建文章

### 功能目标

让用户快速创建一篇新的文字档案。

### 用户入口

- 文章列表页“写文章”按钮
- 侧边栏“写文章”按钮
- 快捷键 `Ctrl/Cmd + N`
- 点击问候语进入写作页

### 页面字段

- 标题输入框
- 正文输入框
- 分类按钮
- 标签按钮
- 取消按钮
- 字数统计
- 保存按钮

### 保存校验

- 标题不能为空。
- 正文不能为空。

### 保存规则

1. 系统读取标题、正文、分类、标签。
2. 系统计算正文字数。
3. 系统生成当前日期。
4. 系统生成 Front Matter。
5. 系统将 Front Matter 与正文拼接为完整 Markdown。
6. 系统根据标题生成安全文件名。
7. 系统保存到当前文章文件夹。
8. 系统清空编辑器和草稿。
9. 系统返回文章列表并刷新。

### 无文件夹兜底

如果无法写入当前文章文件夹，系统会尝试下载 Markdown 文件，提示用户移动到同步文件夹或文章库。

## 8.11 编辑文章

### 功能目标

让用户修改已有文章内容和元数据。

### 用户入口

- 文章详情页“编辑文章”按钮

### 进入编辑时回填

- 标题
- 正文
- 分类
- 标签
- 当前文件名

### 保存规则

- 编辑已有文章时，优先沿用原文件名。
- 保存前桌面端会备份原文件到 `.selfrblog-backups`。
- 保存成功后清空草稿。
- 保存成功后返回列表并重新读取文章。

### 注意事项

当前编辑文章时，即使修改标题，文件名仍沿用原文件名。该行为有利于避免链接或文件引用变化，但可能造成标题和文件名不一致。后续可增加“是否同步重命名文件”的设置。

## 8.12 删除文章

### 功能目标

允许用户从当前文字档案库中移除不再需要的文章。

### 用户入口

- 文章列表卡片删除按钮
- 文章详情页“删除文章”按钮

### 删除流程

1. 用户点击删除。
2. 系统弹出确认框。
3. 用户确认。
4. 桌面端调用删除 API。
5. 存储层将文件移动到 `.selfrblog-trash`。
6. 系统刷新文章列表。

### 当前文案问题

当前确认文案为“此操作不可恢复”，但桌面端实际会移动到回收目录。建议后续改为：

```text
确定要删除这篇文章吗？文章会移动到回收目录，可手动找回。
```

## 8.13 分类管理

### 功能目标

让用户为文章设置一个主要归属主题。

### 用户入口

- 编辑页底部“添加分类”按钮

### 分类来源

- 已加载文章中的已有分类
- 用户新输入的分类

### 交互规则

1. 点击分类按钮。
2. 系统展示分类弹窗。
3. 用户选择已有分类，或点击新增分类。
4. 系统将分类写入隐藏输入字段。
5. 分类按钮显示当前分类名称。
6. 系统保存草稿。

### 当前限制

- 没有独立分类管理页面。
- 无法批量重命名分类。
- 无法删除历史未使用分类，除非修改对应文章。

## 8.14 标签管理

### 功能目标

让用户用多个关键词描述文章，便于之后跨主题检索。

### 用户入口

- 编辑页底部“标签”按钮

### 标签来源

- 已加载文章中的已有标签
- 用户在标签弹窗中新增的标签
- 用户手动输入的标签字符串

### 交互规则

1. 点击标签按钮。
2. 系统展示标签弹窗。
3. 已选标签高亮。
4. 点击标签可选中或取消。
5. 输入新标签并按回车可添加。
6. 弹窗关闭时，系统把标签写入编辑表单。
7. 标签按钮显示当前标签数量。
8. 系统保存草稿。

### 标签格式

保存时，多个标签使用中文或英文逗号分割，然后转换为数组写入 Front Matter。

## 8.15 编辑器 Markdown 快捷键

### 功能目标

提升 Markdown 写作效率。

### 支持快捷键

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl/Cmd + S` | 保存文章 |
| `Ctrl/Cmd + N` | 新建文章 |
| `Ctrl/Cmd + F` | 在列表页聚焦搜索框 |
| `Esc` | 从编辑页或详情页返回列表 |
| `Ctrl/Cmd + B` | 粗体 |
| `Ctrl/Cmd + I` | 斜体 |
| `Ctrl/Cmd + K` | 插入链接 |
| `Ctrl/Cmd + Shift + X` | 删除线 |
| `Ctrl/Cmd + Shift + C` | 行内代码或代码块 |
| `Ctrl/Cmd + Shift + B` | 引用块 |
| `Ctrl/Cmd + Shift + L` | 无序列表 |
| `Ctrl/Cmd + Shift + O` | 有序列表 |
| `Ctrl/Cmd + Shift + H` | 标题 |
| `Ctrl/Cmd + Shift + I` | 图片语法 |
| `Tab` | 插入两个空格或处理缩进 |
| `Shift + Tab` | 减少缩进 |
| `Enter` | 自动延续列表 |

### 自动列表规则

- 在无序列表项后按回车，会自动延续列表。
- 在有序列表项后按回车，会尝试递增序号。
- 在空列表项按回车，会取消该列表项。

## 8.16 字数统计

### 功能目标

让用户实时了解当前正文长度，并为写作日历提供统计数据。

### 统计规则

- 中文字符按单字统计。
- 英文和数字按连续词组统计。
- 正文输入时实时更新。
- 保存文章时将字数写入 `word_count`。

### 展示位置

编辑器底部工具栏右侧。

## 8.17 草稿管理

### 功能目标

防止未保存内容因返回、关闭或误操作丢失。

### 自动保存时机

- 标题变化
- 正文变化
- 分类变化
- 标签变化
- 使用 Markdown 快捷键改变正文
- 从编辑页返回列表

### 草稿恢复流程

1. 系统检测到本地存在草稿。
2. 编辑页显示草稿提示。
3. 用户点击“恢复草稿”。
4. 系统回填标题、正文、分类、标签和正在编辑的文件名。
5. 系统更新字数和标签数量。

### 清除时机

- 文章保存成功后清除草稿。

## 8.18 写作日历

### 功能目标

帮助用户回看长期写作轨迹，观察自己的记录习惯和阶段变化。

### 展示内容

- 年份选择器
- 年度贡献图
- 字数强度图例
- 年度总字数
- 年度写作天数
- 本周字数

### 数据来源

系统根据所有已加载文章的 `date` 和 `wordCount` 计算每日写作字数。

### 统计规则

- 同一天多篇文章字数累加。
- 年度总字数只统计当前选择年份。
- 写作天数指当前年份中有字数记录的日期数。
- 本周字数按当前自然周计算。

### 强度规则

| 每日字数 | 强度 |
| --- | --- |
| 0 | 无 |
| 1-99 | 1 级 |
| 100-299 | 2 级 |
| 300-599 | 3 级 |
| 600 及以上 | 4 级 |

### 年份范围

当前年份往前 5 年到当前年份。

## 8.19 树形头像

### 功能目标

为产品提供私人空间感和识别感，让用户感到这是自己的文字馆。

### 当前能力

- 默认显示 CSS 树形头像或内置树图片。
- 用户可点击树形头像上传自定义图片。
- 自定义图片保存到 `localStorage`。
- 下次启动时恢复自定义图片。

### 后续建议

- 增加“恢复默认头像”。
- 增加头像设置入口。
- 将图片存储从 `localStorage` 迁移到应用数据目录。

## 8.20 问候语

### 功能目标

通过一句简短文字降低工具感，强化私人写作空间气质。

### 当前能力

- 侧边栏展示问候语气泡。
- 点击问候语可进入写作页。

### 后续建议

- 支持用户自定义问候语。
- 根据写作频率或时间显示不同提示。
- 避免过强激励，保持安静和陪伴感。

## 8.21 侧边栏收起与展开

### 功能目标

在浏览和写作之间提供不同的信息密度。

### 交互规则

- 用户点击侧边栏开关，可收起侧边栏。
- 收起后主内容区占据更多宽度。
- 再次点击可恢复侧边栏。
- 进入编辑页时系统自动隐藏侧边栏。

## 9. 异常处理与安全策略

## 9.1 文件夹权限异常

如果系统无法访问用户选择的文件夹，应提示用户重新选择文件夹。

## 9.2 保存异常

保存失败时，系统应展示失败原因。浏览器兼容模式下，如果无法直接写入文件夹，系统会尝试下载 Markdown 文件。

## 9.3 删除异常

删除失败时，系统应保留当前页面状态，并提示失败原因。

## 9.4 路径安全

桌面端存储层使用路径校验，确保文章读取、保存和删除限制在用户选择的文章文件夹内，避免通过特殊文件名访问目录外文件。

## 9.5 静态资源安全

本地 HTTP 服务只允许访问 `public` 目录内的静态资源，防止读取应用外部文件。

## 9.6 隐私原则

当前版本不上传文章内容，不创建账号，不依赖云端服务。文章、草稿、配置和自定义图片均保存在本机。

## 10. 版本功能清单

## 10.1 当前版本已具备

- Windows 桌面客户端
- 本地文件夹选择
- 上次文章文件夹恢复
- Markdown 文件读取
- Front Matter 解析
- 文章列表
- 文章详情
- 新建文章
- 编辑文章
- 保存文章
- 删除文章
- 保存前备份
- 删除到回收目录
- 标题和正文必填校验
- 分类选择和新增
- 标签选择和新增
- 搜索
- 分类筛选
- 标签筛选
- 排序
- 字数统计
- 草稿自动保存
- 草稿恢复
- Markdown 快捷键
- 写作日历
- 年度写作统计
- 树形头像
- 自定义树形头像
- 侧边栏收起展开
- Windows 安装包生成

## 10.2 当前版本限制

- 文章详情不渲染 Markdown，只展示纯文本。
- 不支持公开分享或发布。
- 不支持多设备同步。
- 不支持文章目录递归扫描。
- 不支持独立分类/标签管理。
- 不支持图片资源自动管理。
- 不支持回收站可视化恢复。
- 不支持历史版本可视化恢复。
- 不支持自动更新。
- 当前部分内部命名仍沿用旧项目名。

## 11. 后续规划建议

## 11.1 v1.1：完善当前闭环

- 将产品名、窗口标题、安装包名、配置目录统一更名为“字己”。
- 优化删除确认文案，匹配移动到回收目录的真实行为。
- 增加 Markdown 渲染预览。
- 增加最近打开文件夹。
- 增加恢复默认树形头像。

## 11.2 v1.2：增强档案管理

- 增加回收站页面。
- 增加备份版本查看和恢复。
- 增加分类管理页面。
- 增加标签管理页面。
- 支持文章批量操作。
- 支持文章目录递归扫描。

## 11.3 v1.3：增强写作体验

- 增加主题和字体设置。
- 增加每日写作目标。
- 增加连续写作天数。
- 增加日历按文章下钻查看。
- 增加图片拖拽插入和本地图片管理。

## 11.4 v2.0：开放但不平台化

- 支持导出静态 HTML 归档。
- 支持导出 PDF 或长图。
- 支持本地 Git 版本管理。
- 支持可选同步方案。
- 支持插件化导出。

