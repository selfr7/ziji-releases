# 字己 · 产品功能使用手册

> **字己** 是一款坚持本地优先的个人文字档案应用。用文字记录自己，在回看中认识自己和理解自己，发现生命的留痕。

---

## 目录

1. [开始使用：三步开启文字归档](#1-开始使用三步开启文字归档)
2. [写作与编辑](#2-写作与编辑)
3. [浏览与检索](#3-浏览与检索)
4. [数据管理与安全保障](#4-数据管理与安全保障)
5. [个人空间与个性化](#5-个人空间与个性化)
6. [常见问题（FAQ）](#6-常见问题faq)

---

## 1. 开始使用：三步开启文字归档

字己的设计原则是**极简与无感**——无需注册账号，无需登录，不依赖云端。

### 步骤 1：选择你的文字存档文件夹
首次打开字己时，点击左侧栏的 **「选择文件夹」**，在电脑中指定一个你希望存放文章的本地目录（例如 `D:\我的文字档案` 或 iCloud/OneDrive 同步目录）。

### 步骤 2：写下第一篇文字
点击左侧栏的 **「写文章」**（或按快捷键 `Ctrl + N`），即可进入全屏沉浸写作界面。

### 步骤 3：保存与自动归档
输入标题、分类、标签和正文后，点击右上角的 **「保存」**（或按快捷键 `Ctrl + S`）。文字会立即以标准的 `.md` 文件写入你指定的文件夹中。

---

## 2. 写作与编辑

字己提供了一个安静、专注且高效的写作环境。

### 2.1 沉浸式写作视图
- **自动收起侧边栏**：进入编辑界面时，侧边栏会自动收起，减少视觉干扰，让你全神贯注于眼前的文字。
- **实时字数统计**：右下角实时统计当前文章的正文字数，记录你的每一次敲击。

### 2.2 完整的 Markdown 快捷排版
支持标准的 Markdown 语法，并提供直观的常用快捷键：

| 快捷键 | 功能说明 | 快捷键 | 功能说明 |
|--------|----------|--------|----------|
| `Ctrl + S` | 保存文章到本地 | `Ctrl + N` | 新建空白文章 |
| `Ctrl + B` | **粗体** (`**文本**`) | `Ctrl + I` | *斜体* (`*文本*`) |
| `Ctrl + K` | 插入超链接 (`[链接](url)`) | `Ctrl + Shift + X` | ~~删除线~~ (`~~文本~~`) |
| `Ctrl + Shift + C` | 行内代码 / 代码块 | `Ctrl + Shift + B` | 引用区块 (`> 引用`) |
| `Ctrl + Shift + L` | 无序列表 (`- 列表项`) | `Ctrl + Shift + O` | 有序列表 (`1. 列表项`) |
| `Ctrl + Shift + H` | 标题样式 (`# 标题`) | `Ctrl + Shift + I` | 插入图片语法 |
| `Esc` | 退出编辑，返回文章列表 | `Ctrl + F` | 聚焦到顶部搜索框 |

### 2.3 分类与多标签组织
- **单一分类**：为文章指定所属的主题大类（如：`日记`、`随笔`、`读书复盘`）。
- **灵活多标签**：支持用逗号分隔添加多个标签（如：`情绪, 自我观察, 感恩`），便于从不同维度穿透检索。

### 2.4 草稿实时防丢机制
在编辑打字过程中，字己会在当前窗口会话中实时暂存未保存的内容。即使电脑意外断电或不小心关闭窗口，重新打开应用后也能恢复未保存的草稿，最大程度保护灵感不丢失。点击「保存」或「放弃」后，临时缓存会自动清除。

---

## 3. 浏览与检索

随着时间的推移，文字会逐渐累积成一座丰富的个人档案馆。字己提供了多维度的整理与回看能力。

### 3.1 毫秒级全文快速搜索
- 在顶部搜索框中输入关键词，系统会在 **300ms** 内实时检索所有文章的**标题、正文及标签**。
- 搜索不区分英文字母大小写，输入即出结果。

### 3.2 分类与标签快速筛选
- 左侧栏自动汇总当前文件夹中出现的所有分类与标签。
- 点击任意标签或分类，列表即可秒级过滤出对应主题的文章，方便按专题回看。

### 3.3 灵活的文章排序
支持按以下维度一键切换文章列表排序：
- **按修改时间**：最新编辑的内容排在最前；
- **按创作日期**：按日历时间顺序或倒序回溯；
- **按文章字数**：找出那些长篇沉思或简短随笔。

---

## 4. 数据管理与安全保障

**“数据完全归你所有”** 是字己的核心承诺。

### 4.1 开放且透明的文件存储
字己不使用任何私有数据库打包你的文字。所有内容均以纯文本 `.md` 格式直接保存在本地文件夹中：

```markdown
---
title: "今天的记录"
category: "日记"
tags: ["情绪", "自我观察"]
date: "2026-06-11"
word_count: 300
---

正文内容...
```

- **永久可读**：即使不打开字己，你也可以用任何文本编辑器（VS Code、Typora、Obsidian、系统记事本）直接阅读和修改这些文件。
- **轻松迁移**：换电脑时，只需将这个文件夹拷走即可，无需执行任何复杂的导出操作。

### 4.2 编辑自动历史备份
每次保存已有文章时，字己都会在文章目录的 `.selfrblog-backups/` 隐藏文件夹中自动保留一份修改前的文件快照（按时间戳命名）。如果你不小心误改或覆盖了重要文字，随时可以从中找回历史版本。

### 4.3 安全本地回收站
在字己中点击“删除文章”时，文件**绝不会直接被永久粉碎**，而是会被安全移入文章目录的 `.selfrblog-trash/` 回收站目录中。若需恢复，从该目录移出即可。

---

## 5. 个人空间与个性化

字己不仅是一个工具，更是一个温暖、专属的私人精神角落。

### 5.1 写作活跃日历（热力图）
首页内置类似 GitHub 风格的写作活跃度贡献图。每一天的书写都会点亮一个小方块，并记录当天的写作字数。随着时间的推移，你能直观看到自己思维流动与生命记录的完整轨迹。

### 5.2 每日问候引导语
不知道今天该写些什么时，打开字己，首页会根据时间与心境提供温暖的问候与写作启发引导。点击引导语，即可快速开启今天的自我对话。

### 5.3 多套精致配色主题
内置多套精心调配的视觉主题（浅色优雅、深色沉浸、温暖大地色等），支持在设置中一键切换，满足不同环境下的阅读与书写喜好。

### 5.4 私人相册与空间装扮
支持在首页自定义上传个人喜欢的图片或视觉标识，营造更具归属感与温度的私人书写空间。

---

## 6. 常见问题（FAQ）

### Q1：字己会把我的文章上传到网络吗？
**不会。** 字己是 100% 纯本地运行的应用。没有云端服务器，没有网络同步接口，不收集任何用户输入。你的所有文字仅保存在你自己的硬盘里。

### Q2：我如何在多台电脑之间同步文章？
因为字己的所有数据都是本地标准的 `.md` 文件，你可以直接把文章文件夹建在常见的网盘同步目录中（如 OneDrive、iCloud Drive、坚果云、Dropbox 或自建 Syncthing/NAS）。在另一台电脑的字己中，直接选择同一个同步文件夹即可实现多端无缝同步。

### Q3：我可以把其他软件（如 Obsidian、Typora）的文章放进来吗？
**可以。** 只要是标准的 Markdown（`.md`）文件，放入该文件夹后，在字己中点击“读取文章”即可直接浏览和编辑。

### Q4：如何配合 AI 工具更好地回看和理解自己？
因为所有文章都是纯文本格式，你可以自由选择在本地将文章导入本地大模型工具（如 Claude、GPT 等），让 AI 帮你做结构化总结、梳理过去一年的核心成长课题或发现情绪周期，全流程完全由你自主控制与授权。
